# Polish

SKILL.md opens this file when Classify named `origin=polish` and the task is marketing or app UI. Isolated component: [after-briefing.md](after-briefing.md#isolated-component) instead. Do not open [briefing.md](briefing.md), [after-briefing.md](after-briefing.md), or [dispatch.md](dispatch.md) on this path.

`mode=fast`. Stay-closed list: [load-map.md](load-map.md#polish). Disk owns the look. Lock `style=none`. Do not invent a palette or a catalog `id`.

## This turn

| Branch | When | This turn |
| --- | --- | --- |
| IA named | user named a new job, wrong first fold, or chart-gallery home | Reclassify `origin=redesign`. Close this file. Open [briefing.md](briefing.md#redesign). |
| Focus named | prompt named the problem (spacing, contrast, states, tighten, a11y, distill, copy) | Map onto `focus=` ([Focus](#focus)). Open [redesign.md](redesign.md#craft-audit). |
| Mute | "improve this" / "melhora isso" / "make it better" with no problem | Ask **one** Focus question. End the turn. Markup fails this turn. |

A numbered list of questions in the chat message fails this file. A Briefing card or eight-field dump fails this file.

Done for this turn when the Focus question is the user-facing output, or `focus=` exists and the craft audit has started.

## Focus

One structured question when Focus is blank. Options (seven ids): hierarchy and scan; spacing rhythm; inconsistent color; states (hover, focus, loading); a11y; distill (cut extras the brief did not ask for); copy (microcopy and slogans). Other is their sentence.

Map: `hierarchy` → Hierarchy; `rhythm` → Rhythm; `color` → Color; `states` → States; `a11y` → Harden + States; `distill` → Distill; `copy` → Copy.

Unnamed Constraints is `none`. Do not ask Behave, Look, Scene, Theme, or Palette.

## Slim packet

Write this block once `focus=` exists. Parent keeps it for Implement resume. Omit Sketch occupancy. Schema owner for greenfield/redesign remains [dispatch.md](dispatch.md#valid-packet).

```txt
Intent: job=<from the live page or none>; success=<visible primary or none>
Lock: mode=fast; origin=polish; style=none; focus=<id>; constraints=<quoted or none>
Sketch=none
DESIGN.md: <path on disk, or extracted this pass>
Audit: <P0/P1 lines from the craft audit>
```

`Layout` (join / scan / tracks) is omitted. `character=`, `folds=`, `style_path`, and catalog ids are `none`.

Valid when `origin=polish`, `style=none`, `Sketch=none`, `focus=` is one of the seven ids, `DESIGN.md` is on disk (or tokens were extracted from current CSS this pass), and Audit names at least one P0 or P1. Empty "looks more polished" fails.

## Work

Same window. Sequential.

1. **Audit.** [redesign.md](redesign.md#craft-audit). Zero visual edits until the ranked list exists. DESIGN.md missing: extract tokens from current CSS ([design-md.md](design-md.md)); do not invent a palette. Done when the slim packet is valid.
2. **Implement.** [implement.md](implement.md) plus [load-map.md](load-map.md#polish) Implement rows. Patch existing files. Keep the current layout family. Close Audit P0/P1. Skip marketing `See:` / `tracks=` / `scale=`. Skip app UI recipe pick. Return the [verification.md](verification.md) block.
3. **QA slim.** Do not edit. [anti-slop.md](anti-slop.md) Always + polish branch (skip category-reflex and logo-swap). [preflight-checklist.md](preflight-checklist.md) **Tier A** only. Skip [crit.md](crit.md). [visual-rubric.md](visual-rubric.md): skip Structural necessity and Domain specificity. Return the verification block and a P0/P1 table for remaining craft fails.
4. **Fix.** One Implement resume with that table. Re-run QA slim only if a P0 remains.

## File done

`focus=` exists. Slim packet is valid. Markup patched on the current stack. Verification block exists. Tier A boxes checked or failed and fixed. Anti-slop Always has run. P0 count is 0. Catalog, composition, and briefing stayed closed.
